# Dependencias
node_modules/
.pnp
.pnp.js

# Builds
dist/
dist-electron/
build/
out/
release/
.expo/
.expo-shared/
web-build/
*.apk
*.aab
*.ipa

# Claves y secretos — NUNCA se commitean
.env
.env.*
!.env.ejemplo
*.keystore
*.jks
credenciales/
google-services.json
supabase/.temp/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Sistema
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/*
!.vscode/extensions.json
.idea/

# Cache
.cache/
.turbo/
*.tsbuildinfo

# Render de prueba del talonario (se regenera, no se versiona)
nota-pedido-prueba.html

# Probador: se genera con `npm run probador` y lleva la config del .env dentro
herramientas/probador/probador.html
# Cargador de un solo uso: lleva la config del .env y 300 KB de catálogo
herramientas/probador/cargar-catalogo.html

# Bundles de SQL que se regeneran (no se versionan)
supabase/PENDIENTES.sql

# ID de dispositivo del probador: se genera una vez y no se versiona
herramientas/probador/.instalacion-id
__pycache__/

# Salida del probador para publicar (se genera en cada despliegue)
publico/
